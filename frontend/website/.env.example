# Base URL of the backend API (no trailing slash)
# Must be prefixed NEXT_PUBLIC_ since it's used in client components (the
# browser needs to call the API directly from pages like Cart/Checkout).
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000/api
